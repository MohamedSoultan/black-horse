import 'package:flutter_test/flutter_test.dart';import 'package:black_horse/l10n/app_localizations.dart';import 'package:flutter/material.dart';void main(){test('Arabic and English translations exist',(){expect(AppLocalizations(const Locale('ar')).login,'تسجيل الدخول');expect(AppLocalizations(const Locale('en')).login,'Login');});test('supported locales include RTL Arabic',(){expect(AppLocalizations.supportedLocales,contains(const Locale('ar')));});}
