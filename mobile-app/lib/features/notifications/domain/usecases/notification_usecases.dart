import '../notification_repository.dart';class GetNotifications{GetNotifications(this.repo);final NotificationRepository repo;call()=>repo.list();}class GetUnreadCount{GetUnreadCount(this.repo);final NotificationRepository repo;call()=>repo.unreadCount();}class MarkNotificationRead{MarkNotificationRead(this.repo);final NotificationRepository repo;call(String id)=>repo.markRead(id);}
