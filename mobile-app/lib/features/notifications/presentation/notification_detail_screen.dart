import 'package:flutter/material.dart';import '../domain/notification_entity.dart';class NotificationDetailScreen extends StatelessWidget{const NotificationDetailScreen({super.key,required this.notification});final NotificationEntity notification;Widget build(BuildContext c)=>Scaffold(appBar:AppBar(title:const Text('Notification')),body:ListView(padding:const EdgeInsets.all(24),children:[Text(notification.title,style:Theme.of(c).textTheme.displaySmall),const SizedBox(height:16),Text(notification.message,style:Theme.of(c).textTheme.bodyLarge),if(notification.data!=null)Padding(padding:const EdgeInsets.only(top:24),child:Text('Reference: ${notification.data}'))]));}
