# Setup Guide

Copy the repository `.env.example` to `.env`, then run `docker compose up --build`. The API health endpoint is `GET /api/v1/health`. Install Flutter and run `flutter pub get` inside `mobile-app` for mobile development.
