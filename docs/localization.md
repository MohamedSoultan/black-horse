# Localization and UI foundation

Arabic is the default locale and English is supported. `LocaleNotifier` persists the selected language in secure storage; `MaterialApp` consumes it and applies Flutter's RTL behavior for Arabic automatically. Shared UI components live in `core/presentation/ui_components.dart`.
