# Provider workflow

An authenticated registered user submits one pending application. Admin or Super Admin reviews it. Approval creates a verified provider profile and grants the `PROVIDER` role. Rejection preserves the application and records a reason. Only verified providers are returned by public directory endpoints.
